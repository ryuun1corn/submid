import AuthenticationCard from '@/components/elements/AuthenticationCard/AuthenticationCard';
import CreateFormCard from './module-elements/CreateFormCard';

const CreateFormModule = () => {
  return (
    <AuthenticationCard>
      <CreateFormCard />
    </AuthenticationCard>
  );
};

export default CreateFormModule;
